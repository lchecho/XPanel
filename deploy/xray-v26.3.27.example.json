{
  "_comment": "XPanel 基础配置样例。面板在运行时通过管理接口为每个用户创建专属 SS2022 入站，因此这里不需要预配置任何入站。",
  "log": {
    "loglevel": "warning"
  },
  "api": {
    "_comment": "tag 必须非空，否则 Xray 拒绝启动（API tag can't be empty）",
    "tag": "api",
    "listen": "127.0.0.1:10085",
    "services": ["HandlerService", "StatsService"]
  },
  "stats": {},
  "policy": {
    "_comment": "面板依赖用户级计数器计费；缺少这两项时配额永远不会触发，面板会把入站模板判为不兼容",
    "levels": {
      "0": {
        "statsUserUplink": true,
        "statsUserDownlink": true
      }
    }
  },
  "inbounds": [],
  "outbounds": [
    {"protocol": "freedom", "tag": "direct"}
  ]
}
